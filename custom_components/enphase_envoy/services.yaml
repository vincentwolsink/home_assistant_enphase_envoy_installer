get_grid_profiles:
set_grid_profile:
  fields:
    profile:
      required: true
      example: "EN 50549-1:2019 RfG E02 Netherlands:1.2.4"
      selector:
        text:
upload_grid_profile:
  fields:
    file:
      required: true
      example: "/share/grid_profiles/64d31e5868ac391817b21af1"
      selector:
        text:
enable_dpel:
  description: Enable DPEL (Dynamic Power Export Limit)
  fields:
    watts:
      description: Optional power limit in watts (default is 50W if not specified)
      example: 50
      required: false
disable_dpel:
  description: Disable DPEL (Dynamic Power Export Limit)

